@if ((this.AssignedRenderMode == null) || !this.RendererInfo.IsInteractive) @* only for Server-Side Rendering and prerendering *@
{
    <script type="text/javascript" nonce="@Nonce">
        @((MarkupString)GetApplicationInsightsScript())
        @((MarkupString)GetDefaultTelemetryInitializerScript())
        @((MarkupString)GetInitialTrackPageViewScript())
    </script>
}