@attribute [Route("/components/HxDialog")]
@attribute [Route("/components/" + nameof(HxDialogBase<object>))]

<ComponentApiDoc Type="typeof(HxDialogBase<object>)">

	<h3 id="Implementation">Imlementation</h3>
	<p>Use <a href="/components/@nameof(HxModal)">@nameof(HxModal)</a> with <code>@@ref="@nameof(HxDialogBase<object>.modal)"</code> inside of the dialog. See demo.</p>

	<h3 id="UsageOfCustomDialog">How to use a custom dialog</h3>
	<Demo Type="typeof(HxDialogBase_Demo)" />

	<h3 id="ImplementationOfCustomDialog">How to implement a custom dialog</h3>
	<Demo Type="typeof(CustomDialog)" Tabs="false" />

</ComponentApiDoc>
