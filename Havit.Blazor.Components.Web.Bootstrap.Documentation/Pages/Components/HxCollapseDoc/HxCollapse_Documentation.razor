@attribute [Route("/components/" + nameof(HxCollapse))]
@attribute [Route("/components/" + nameof(HxCollapseToggleButton))]
@attribute [Route("/components/" + nameof(HxCollapseToggleElement))]

<ComponentApiDoc Type="typeof(HxCollapse)">

	<h3 id="BasicUsage">Basic Usage</h3>
	<Demo Type="typeof(HxCollapse_Demo_Basic)" Tabs="false" />

	<h3 id="Horizontal">Horizontal</h3>
	<p>You can change the animation direction with <code>CollapseDirection</code> parameter.</p>
	<Demo Type="typeof(HxCollapse_Demo_Horizontal)" />

	<h3 id="MultipleTargets">Multiple targets</h3>
	<p>You can use the <code>CollapseTarget</code> selector to toggle multiple targets.</p>
	<Demo Type="typeof(HxCollapse_Demo_MultipleTargets)" />

	<h3 id="CustomToggleElement">Custom toggle element</h3>
	<p>Use <code>HxCollapseToggleElement</code> (<code>with ElementName="..."</code>) instead of <code>HxCollapseToggleButton</code> to create custom toggler (usually you will add <code>role="button"</code> to apply pointer cursor type).</p>
	<Demo Type="typeof(HxCollapse_Demo_CustomToggleElement)" />

	<h3 id="Programming">Programming</h3>
	<Demo Type="typeof(HxCollapse_Demo_Programming)" />

</ComponentApiDoc>

<ComponentApiDoc Type="typeof(HxCollapseToggleButton)" />

<ComponentApiDoc Type="typeof(HxCollapseToggleElement)" />