<HxInputNumber Label="No decimals (InputMode defaults to Numeric)" Decimals="0" TValue="float" @bind-Value="enteredNumber" />
<HxInputNumber Label="Decimals (InputMode not set)" Decimals="1" TValue="float" @bind-Value="enteredNumber" />
<HxInputNumber Label="Explicit InputMode.Decimal" Decimals="1" TValue="float" @bind-Value="enteredNumber" InputMode="InputMode.Decimal" />

@code {
    private float enteredNumber;
}
