@attribute [Route("/components/" + nameof(HxGoogleTagManager))]
@attribute [Route("/components/" + nameof(HxGoogleTagManagerPageViewTracker))]
@using Havit.Blazor.GoogleTagManager

<ComponentApiDoc Type="typeof(HxGoogleTagManager)">
	<h3>Basic usage</h3>
	<p>Install GTM support through application statup:</p>
	<CodeSnippet File="~\Pages\Components\HxGoogleTagManagerDoc\HxGoogleTagManager_ServiceRegistration.CodeSnippet.cs" />
</ComponentApiDoc>

<ComponentApiDoc Type="typeof(HxGoogleTagManagerPageViewTracker)">
	<h3>Basic usage</h3>
	<p>Install GTM support through application statup (see above) and add <code>HxGoogleTagManagerPageViewTracker</code> to <code>App.razor</code> or <code>MainLayout.razor</code></p>
	<CodeSnippet Language="html">@("<HxGoogleTagManagerPageViewTracker />")</CodeSnippet>
</ComponentApiDoc>