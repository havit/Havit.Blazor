@attribute [Route("/components/" + nameof(HxFilterForm<string>))]

<ComponentApiDoc Type="typeof(HxFilterForm<>)">
	
	<SectionTitle Id="UsedInHxListLayout">Used in @nameof(HxListLayout)</SectionTitle>
	<p>
		See <a href="/components/@nameof(HxListLayout)">@nameof(HxListLayout)</a> demo with its filter and chips
		to see the real usage of the <code>HxFilterForm</code>.
	</p>

	<SectionTitle Id="StandaloneDemo" />
    <DocAlert Type="DocAlertType.Warning">
        <code>HxFilterForm</code> component has to be kept rendered to work propertly with <code><a href="/components/HxChipList">HxChipList</a></code>.<br/>
        Beware of <code><a href="/components/HxOffcanvas">HxOffcanvas</a></code> where you have to set <code>RenderModer="OffcanvasRenderModer.Always"</code> to prevent removing the component from rendering when offcanvas is closed.
    </DocAlert>
	<Demo Type="typeof(HxFilterForm_Demo)" />

</ComponentApiDoc>
