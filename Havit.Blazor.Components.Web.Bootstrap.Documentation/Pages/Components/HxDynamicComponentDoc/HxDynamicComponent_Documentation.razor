@attribute [Route("/components/" + nameof(HxDynamicElement))]

<ComponentApiDoc Type="typeof(HxDynamicElement)">
    <p>Allows dynamic composition of more complex components. Used in some of the Hx-components internally, but also exposed for your needs.</p>

    <h3>BasicUsage</h3>
    <Demo Type="typeof(HxDynamicElement_Demo_BasicUsage)" Tabs="false" />

</ComponentApiDoc>