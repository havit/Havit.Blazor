<div>
	<div class="d-lg-flex">
		<div class="title @NamedViewsSectionCssClass">
			<span role="button" @onclick="ToggleNamedViewsSectionExpanded">
				@if (TitleSection != null)
				{
					@TitleSection
				}
				else if (!String.IsNullOrEmpty(Title))
				{
					@Title
				}
			</span>
			@if (NamedViewsSection != null)
			{
				@NamedViewsSection
			}
		</div>

		@if (SearchSection != null)
		{
			<div class="search mb-3 mb-lg-0">
				@SearchSection
			</div>
		}

		<div class="commands text-right">
				@if (FilterSection != null)
				{
					<HxButton OnClick="ToggleFilterExpanded">Filtrovat</HxButton>
				}
			<HxButton OnClick="ToggleDrawerExpanded">Otevřít Drawer</HxButton>
			@if (CommandsSection != null)
			{
				@CommandsSection
			}
		</div>
	</div>

	@if (FilterSection != null)
	{
		<CascadingValue Value="@FilterExpanded" Name="FilterExpanded"> @* TODO pojmenování, konstanta *@ @* Kam může programátor napsat, že chce filtr by default expandovaný? *@
			@FilterSection
		</CascadingValue>
	}

	@if (DataSection != null)
	{
		<div>@DataSection</div>
	}

	@if (DetailSection != null)
	{
		@*TODO: Otevření jen, je-li vybrán záznam pro zobrazení detailu. Používání detailu zapnuto použitím sekce? *@
		@*TODO: Kdo řídí vybrání detailu?*@
		<div class="drawer p-4 @DrawerCssClass">
			<button class="close" @onclick="ToggleDrawerExpanded">&times;</button>
			@DetailSection
		</div>
	}
</div>