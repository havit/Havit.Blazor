@namespace Havit.Blazor.Components.Web.Bootstrap
<div class="@CssClassHelper.Combine("hx-chip-list", this.CssClassEffective)">
	@if (Chips != null)
	{
		foreach (Havit.Blazor.Components.Web.Bootstrap.ChipItem chipItem in Chips)
		{
			<HxBadge @key="@chipItem" Settings="this.ChipBadgeSettingsEffective">
				@chipItem.ChipTemplate
				@if (chipItem.Removable)
				{
					<span role="button" class="hx-chip-list-remove-btn" @onclick="() => InvokeOnChipRemoveClickAsync(chipItem)" @onclick:stopPropagation>
						<HxIcon Icon="BootstrapIcon.X" />
					</span>
				}
			</HxBadge>
		}
		@if (Chips.Any() && this.ShowResetButtonEffective)
		{
			@if (ResetButtonTemplate is not null)
			{
				@ResetButtonTemplate
			}
			else
			{
				<span role="button" class="hx-chip-list-reset-btn" @onclick="() => InvokeOnResetClickAsync()" @onclick:stopPropagation>
					@ResetButtonText
					<HxIcon Icon="BootstrapIcon.X" />
				</span>
			}
		}
	}
</div>
