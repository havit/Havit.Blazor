@attribute [Route("/components/" + nameof(HxNav))]
@attribute [Route("/components/" + nameof(HxNavLink))]

<ApiDoc Type="typeof(HxNav)">
    <DocHeading Title="Base Nav" />
    <Demo Type="typeof(HxNav_Demo_BaseNav)" />

    <DocHeading Title="Vertical" />
    <Demo Type="typeof(HxNav_Demo_Vertical)" />

    <DocHeading Title="Tabs" />
    <p>The raw implementation requires you to set the <code>active</code> class manually (the <code>Match</code> behavior on <code>Href</code> might help).</p>
    <p>See <a href="/components/@nameof(HxTabPanel)">@nameof(HxTabPanel)</a> for a more sophisticated implementation.</p>
    <Demo Type="typeof(HxNav_Demo_Tabs)" />

    <DocHeading Title="Underline" />
    <p>Use <code>Variant="NavVariant.Underline"</code> for an underlined style:</p>
    <Demo Type="typeof(HxNav_Demo_Underline)" />

    <DocHeading Title="Pills" />
    <Demo Type="typeof(HxNav_Demo_Pills)" />

    <DocHeading Title="Menus" />
    <Demo Type="typeof(HxNav_Demo_Menus)" />

    <DocHeading Title="Accessibility" />
    <p>
        Set the <code>Match</code> parameter on <code>HxNavLink</code> so the active link receives the <code>active</code> class and the current location is perceivable.
        Disabled links (<code>Enabled="false"</code>) are rendered with <code>aria-disabled="true"</code> and removed from the tab order (<code>tabindex="-1"</code>).
        For tabbed interfaces that swap panels, prefer <a href="/components/@nameof(HxTabPanel)">@nameof(HxTabPanel)</a>, which manages the
        <code>role="tab"</code>/<code>tabpanel</code> and <code>aria-selected</code> semantics for you.
    </p>
</ApiDoc>

<ApiDoc Type="typeof(HxNavLink)" />
