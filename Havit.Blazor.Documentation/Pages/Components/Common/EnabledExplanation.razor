Can be used to render an individual component as <code>disabled</code> or override the state from @nameof(HxFormState). To disable multiple components, use <code><a href="/components/HxFormState">@nameof(HxFormState)</a></code>.