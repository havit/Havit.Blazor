@attribute [Route("/components/" + nameof(HxDialogBase<TResult>))]

<ApiDoc Type="typeof(HxDialogBase<TResult>)">

    <DocHeading Title="Implementation" />
    <p>Use <a href="/components/@nameof(HxDialog)">@nameof(HxDialog)</a> with <code>@@ref="@nameof(HxDialogBase<object>.dialog)"</code> inside the dialog. See demo.</p>

    <DocHeading Title="How to use a custom dialog" Id="custom-dialog-usage" />
    <Demo Type="typeof(HxDialogBase_Demo)" />

    <DocHeading Title="How to implement a custom dialog" Id="custom-dialog-implementation" />
    <Demo Type="typeof(CustomDialog)" Tabs="false" />

</ApiDoc>
